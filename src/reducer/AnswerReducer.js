const AnswerReducer = (state, action) => {
  switch (action.type) {
    case "UPDATE_ANSWER":
      console.log(state);
      break;
    //localStorage.setItem("DATA_ANSWER",)
    default:
      break;
  }
};
